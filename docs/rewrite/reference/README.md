# Visual reference

`design-direction.webp` is a **directional mockup**, not a pixel-perfect contract.

Use it for mood, density, hierarchy, layering and responsive intent. If it conflicts with the written specs, the written specs win.
